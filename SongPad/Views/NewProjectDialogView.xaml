<UserControl x:Class="SongPad.Views.NewProjectDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SongPad.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300"
			 Padding="10">
	<StackPanel>
		<TextBlock Text="Project name" Margin="0,0,0,10" />
		<TextBox Width="300" Height="25" VerticalContentAlignment="Center" Text="{Binding ProjectName, Mode=OneWayToSource}" />
		<Button Content="OK" Command="{Binding OkCommand}" HorizontalAlignment="Right" Width="60" Margin="0,10,0,0" />
	</StackPanel>
</UserControl>
